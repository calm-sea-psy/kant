1. 채팅 템플릿(chat template)
[{role, content}, ...] 형태의 메시지 목록을, 모델이 실제로 학습한 하나의 토큰 시퀀스 문자열로 변환하는 규칙이다. 모델마다 다르다.

모델은 사전학습·명령어 튜닝 때 "역할 경계를 특정 특수 토큰(special token)으로 표시한" 데이터로 배운다. 추론 때 그 형식을 그대로 재현해야 성능이 나온다. 형식이 틀어지면 모델이 지시를 무시하거나, 서두를 붙이거나, 종료를 못 한다.

ㄱ) 구성 요소

| 요소 | 역할 |
|---|---|
| BOS / EOS 토큰 | 시퀀스 시작·끝. 턴 종료 표시에도 쓰임 |
| 역할 표지(role marker) | system / user / assistant 구간을 여는·닫는 특수 토큰 |
| 시스템 메시지 자리 | 지시·페르소나. 모델에 따라 별도 슬롯이거나 첫 user에 합쳐짐 |
| 생성 프롬프트(generation prompt) | 맨 끝에 "이제 assistant가 말할 차례"라는 헤더를 붙임 |

ㄴ) 사용 방법
템플릿은 tokenizer_config.json의 chat_template 필드에 Jinja2 문자열로 들어 있고, 토크나이저가 적용한다. 직접 문자열을 만들지 말고 항상 이 함수를 쓴다.

    messages = [
        {"role": "system", "content": "You are helpful."},
        {"role": "user",   "content": "Hi"},
    ]

    # 추론용 — 끝에 assistant 헤더를 붙여 모델이 생성을 시작하게
    prompt = tokenizer.apply_chat_template(
        messages, tokenize=False, add_generation_prompt=True,
    )

    - add_generation_prompt=True: 추론 시. 마지막에 <|im_start|>assistant\n 같은 생성 프롬프트를 추가
    - add_generation_prompt=False: 이미 assistant 응답이 목록에 있는 학습 데이터를 만들 때

ㄷ) 실패

| 사례 | 증상 |
|---|---|
| 템플릿 없이 수동 문자열 조립 | 공백·특수 토큰 하나 어긋나 지시 무시, 서두("Sure!") 삽입 |
| BOS 중복 | 토크나이저가 BOS를 또 붙임 → 첫 토큰 분포 왜곡 |
| 학습/추론 템플릿 불일치 | SFT는 ChatML로 했는데 서빙은 Llama 형식 → 형식 준수율 급락, 종료 실패 |
| 베이스 모델에 시스템 슬롯이 없는데 시스템 메시지 전달 | 조용히 무시되거나 오류 |
| base(비instruct) 모델에 채팅 템플릿 없음 | apply_chat_template가 오류 → 직접 하나를 정해 일관되게 사용 |
| 같은 베이스의 다른 파인튜닝이 다른 템플릿 사용 | 모델 카드·tokenizer_config.json 확인 필수 |

    - 항상 apply_chat_template — 수동 조립 금지
    - 미세조정 시 베이스 모델의 템플릿과 정확히 일치시킨다. 베이스에 없으면 하나(보통 ChatML)를 골라 학습·평가·서빙에서 동일하게
    - 검증: 토큰화한 예시를 다시 디코딩해 눈으로 확인 — print(tokenizer.decode(tokenizer.apply_chat_template(messages)))
    - 도구 호출(tool calling)을 쓰면 템플릿이 도구 정의·결과 메시지까지 직렬화하는 방식도 모델마다 다르므로 같이 확인
    - 템플릿은 tokenizer.chat_template에 문자열을 할당해 덮어쓸 수 있다 — 커스텀 형식이 필요할 때

2. Forward pass와 generate의 차이
순전파(forward pass)는 모델을 1회 실행해 모든 위치의 로짓(logits)을 얻는 것이고, generate는 그 순전파를 자기회귀(autoregressive)로 반복하는 루프다.

ㄱ) 순전파(forward pass)
입력 토큰 시퀀스를 모델에 한 번 통과시켜 각 위치의 다음 토큰 분포를 얻는다.

    out = model(input_ids)                  # out.logits: [배치, 길이, 어휘크기]
    next_token_logits = out.logits[:, -1]   # 마지막 위치의 분포 하나
    loss = out.loss                         # labels 를 주면 여기서 계산

    - 모든 레이어를 한 번 지나는 순수 텐서 연산. 토큰 선택도, 루프도 없음
    - 미분 가능 → 뒤에 역전파(backward)를 붙여 학습
    - 학습 시 모든 위치를 병렬로 계산 (교사 강요(teacher forcing) — 모델이 자기 예측이 아니라 정답 이전 토큰을 봄)
    - 쓰임: 학습, 손실·당혹도(perplexity) 계산, 채점, 분류. 분류는 generate 없이 순전파 1회로 라벨 토큰들의 로짓만 비교하면 된다(라벨 매핑(verbalizer) 방식) — 더 빠르고 결정적이며 형식 파싱이 필요 없다

ㄴ) generate
순전파를 반복하며 토큰을 하나씩 이어 붙인다.

a. 모양

    ids = model.generate(
        input_ids,
        max_new_tokens=128,
        do_sample=True, temperature=0.7, top_p=0.9,
        eos_token_id=tokenizer.eos_token_id,
    )                                       # → 완성된 토큰 시퀀스

내부 루프

    ids, past = input_ids, None
    for _ in range(max_new_tokens):
        out = model(ids[:, -1:] if past else ids,
                    past_key_values=past, use_cache=True)
        past = out.past_key_values
        logits = out.logits[:, -1, :]        # 마지막 위치
        next_id = logits.argmax(-1, keepdim=True)   # 탐욕적 디코딩(greedy)
        ids = torch.cat([ids, next_id], dim=-1)
        if next_id.item() == eos_id:
            break

b. generate가 순전파 위에 얹는 것

| 요소 | 내용 |
|---|---|
| 디코딩 전략(decoding strategy) | 탐욕적, 샘플링(temperature·top-k·top-p), 빔 탐색(beam search) |
| 종료 조건(stopping criteria) | EOS 토큰, max_new_tokens, stop 문자열 |
| KV 캐시(KV cache) | 이전 토큰의 키·값을 저장해 매 스텝 새 토큰만 계산 |
| 로짓 후처리 | 반복 페널티, 금지 단어, 최소 길이 |

    - 토큰 선택(argmax·샘플링)에서 경사가 끊겨 end-to-end로 미분 불가 → 표준 학습 루프에는 안 씀 (RLHF 같은 온라인 방식은 롤아웃(rollout) 생성에 generate를 호출)
    - max_length는 입력과 출력 전체 길이를 제한한다. max_new_tokens는 새로 생성할 토큰 수만 제한한다

c. generate 안의 두 단계

| 단계 | 하는 일 | 병목 |
|---|---|---|
| 프리필(prefill) | 첫 순전파로 프롬프트 전체를 한 번에 처리 → KV 캐시 채우고 첫 토큰 생성 | 연산 (compute-bound) |
| 디코드(decode) | 이후 순전파마다 토큰 1개씩, KV 캐시 읽으며 진행 | 메모리 대역폭 (memory-bound) |

    - 출력 토큰 N개짜리 generate 1회 = 프리필 1회 + 디코드 (N−1)회. 긴 출력의 지연·비용은 대부분 디코드 단계에서 발생한다

d. 순전파 vs generate 정리

| 구분 | 순전파(forward pass) | generate |
|---|---|---|
| 무엇 | 모델 1회 호출 | 순전파를 반복하는 루프 |
| 출력 | 로짓 [B, T, V] | 토큰 시퀀스 |
| 자기회귀 | 없음 | 있음 |
| 반복 횟수 | 1 | 최대 max_new_tokens |
| 디코딩 전략 | 해당 없음 | greedy / sampling / beam … |
| KV 캐시 | 보통 미사용 | 필수 |
| 미분 가능 | 예 (학습 가능) | 아니오 |
| 주 용도 | 학습, 손실·확률, 분류·채점 | 텍스트 생성 |
| 비용 | 1 pass | 프리필 1 + 디코드 (N−1) |

3. 자기회귀(autoregressive)의 원리
언어모델은 시퀀스 확률을 연쇄 법칙으로 분해한다. 각 토큰은 앞선 모든 토큰에 조건부다. 생성은 이 곱을 왼쪽에서 오른쪽으로 한 항씩 밟아 나가는 것 — 토큰을 하나 뽑아 시퀀스에 붙이고, 그 확장된 시퀀스로 다음 토큰을 뽑는다. 학습 때부터 인과 마스크(causal mask)로 미래 토큰을 못 보게 했기 때문에 이 순서가 강제된다.

ㄱ) 루프 한 스텝
    - 순전파 — 현재 시퀀스를 모델에 통과시켜 마지막 위치의 로짓(logits)을 얻는다
    - 로짓 후처리 — 온도(temperature) 나누기, 반복 페널티(repetition penalty), top-k / top-p 필터, 로짓 편향(logit bias), 최소 길이 전에는 EOS 억제
    - 확률화 + 선택 — softmax로 확률 분포를 만들고 다음 토큰을 선택 (탐욕적이면 argmax, 아니면 샘플링)
    - 덧붙이기 — 선택된 토큰을 시퀀스 끝에 append
    - 종료 조건 확인 — EOS 토큰 / max_new_tokens 도달 / stop 문자열 매칭이면 중단
    - 아니면 처음으로 — 단, 다음 순전파는 새 토큰 1개만 넣는다 (아래 KV 캐시)

    def generate(model, prompt_ids, max_new_tokens, eos_id,
                temperature=1.0, top_p=1.0):
        ids = prompt_ids

        # 프리필: 프롬프트 전체를 한 번에 → KV 캐시 채우고 첫 로짓
        out = model(ids, use_cache=True)
        kv = out.past_key_values

        for _ in range(max_new_tokens):
            logits = out.logits[:, -1, :]              # 1) 마지막 위치

            logits = logits / temperature             # 2) 후처리
            logits = top_p_filter(logits, top_p)

            probs = softmax(logits, dim=-1)           # 3) 확률화
            next_id = multinomial(probs, 1)           #    선택 (greedy면 argmax)

            ids = cat([ids, next_id], dim=-1)         # 4) 덧붙이기

            if next_id.item() == eos_id:              # 5) 종료
                break

            out = model(next_id,                      # 6) 새 토큰 1개만
                        past_key_values=kv, use_cache=True)
            kv = out.past_key_values
        return ids

ㄴ) KV 캐시(KV cache)가 루프를 바꾼다
    - 순진한 구현: 매 스텝 전체 시퀀스를 다시 순전파 → t번째 토큰에서 길이 t를 처리, 누적 O(n²)
    - KV 캐시: 각 레이어 어텐션의 키·값을 저장해 둔다. 새 토큰은 자기 쿼리(Q)만 만들고 캐시된 K·V 전체와 어텐션 → 매 스텝 토큰 1개만 모델을 통과
    - 프리필(prefill): 프롬프트 전체로 캐시를 한 번에 채우는 첫 순전파
    - 디코드(decode): 이후 스텝. 토큰당 전체 파라미터를 메모리에서 읽어야 해서 메모리 대역폭 병목
    - 캐시 크기 ≈ 2 × 레이어 수 × 헤드 수 × 헤드 차원 × 시퀀스 길이 × 배치 × 정밀도(byte) — 길어질수록 메모리 압박

ㄷ) 디코딩 전략(decoding strategy)

| 전략 | 방식 | 특징 |
|---|---|---|
| 탐욕적(greedy) | 매 스텝 argmax | 결정적, 반복·단조로움 위험 |
| 온도(temperature) | 로짓을 T로 나눔 | T<1 뾰족, T>1 평평, T→0은 greedy |
| top-k | 상위 k개만 남기고 재정규화 | k 고정이라 분포 모양에 둔감 |
| top-p (핵 샘플링, nucleus) | 누적 확률 p까지 최소 집합만 | 분포에 적응적, 가장 흔함 |
| 빔 탐색(beam search) | b개 가설을 병렬 유지, 시퀀스 확률 최대화 | 번역·요약엔 좋고 열린 생성엔 밋밋·반복 |
| 반복 억제 | repetition penalty, no-repeat n-gram | 루프 탈출 |

    - 평가에서는 재현성을 위해 T=0(탐욕적)을 쓴다

ㄹ) 종료 조건(stopping criteria)

| 조건 | 설명 |
|---|---|
| EOS 토큰 | 모델이 스스로 생성. 정상 종료 |
| max_new_tokens / max_length | 하드 컷. 형식이 잘릴 수 있음 |
| stop 문자열 | 디토큰화(detokenize) 후 매칭 — 토큰 경계와 안 맞을 수 있어 후처리 필요 |
| min_new_tokens | 너무 일찍 끝나지 않게 그 전까지 EOS 로짓을 −∞로 |

    배치 생성의 복잡성
        - 시퀀스마다 EOS 시점이 달라 → 완료된 슬롯을 마스킹하고 패딩
        - 생성은 오른쪽으로 자라므로 입력은 왼쪽 패딩(left padding)
        - 위치 인코딩(positional encoding, RoPE 등)은 새 토큰마다 위치를 갱신. 학습 최대 길이를 넘으면 외삽 문제
        - 서빙 엔진은 연속 배칭(continuous batching) — 완료된 슬롯에 대기 중인 새 요청을 즉시 투입해 GPU를 놀리지 않음
        - 추측 디코딩(speculative decoding): 작은 초안 모델이 여러 토큰을 미리 뽑고 본 모델이 한 번의 순전파로 검증 → 스텝당 여러 토큰

4. Greedy decoding (탐욕적 디코딩)
매 스텝에서 확률이 가장 높은 토큰 하나(argmax, top-1)를 무조건 고르는 방식이다. 모델은 각 위치에서 어휘 전체에 대한 확률분포를 내놓는데, greedy는 그중 1위만 뽑아 이어 붙이고 다음 스텝으로 넘어간다.

    "오늘 날씨가" → [맑다 0.55, 흐리다 0.20, 춥다 0.15, ...] → "맑다" 선택
    "오늘 날씨가 맑다" → [. 0.4, 그래서 0.3, ...] → "." 선택

    - 결정적(deterministic): 같은 입력 → 항상 같은 출력. 재현이 쉬움
    - 근시안적(greedy): "지금 이 스텝에서 1위"가 "문장 전체로 봤을 때 최선"이라는 보장이 없다. 초반에 확률 0.51짜리를 골랐다가 뒤에서 막히는 경우를 되돌리지 못한다. 이 약점을 보완하려고 상위 k개 경로를 동시에 들고 가는 빔서치(beam search)가 나왔다
    - 반복 루프에 잘 빠짐: 한번 높은 확률 궤도에 올라타면 계속 그쪽을 골라서 같은 구절을 되풀이하는 degeneration이 흔함
    - 속도는 sampling과 거의 같음 (뽑는 연산만 조금 가벼움)

5. Sampling (샘플링)
확률분포에서 확률에 비례해 무작위로 하나를 뽑는 방식이다. 1위가 0.55면 55% 확률로 그게 뽑히고, 3위(0.15)도 15% 확률로 뽑힌다. 그냥 뽑으면 꼬리에 있는 이상한 토큰이 우연히 걸릴 수 있어서, 보통 세 가지 손잡이로 분포를 다듬는다.

| 손잡이 | 하는 일 |
|---|---|
| temperature | 분포의 뾰족함 조절. logits를 T로 나눈 뒤 softmax. T<1이면 뾰족(보수적), T>1이면 평평(과감), T→0이면 greedy와 같아짐 |
| top-k | 확률 상위 k개 토큰만 후보로 남기고 그 안에서 샘플링 |
| top-p (nucleus) | 확률을 큰 순으로 더해 누적 p(예: 0.9)를 넘는 지점까지만 후보. 후보 개수가 상황에 따라 자동 조절 → top-k의 약점 보완, 현재 열린 생성의 사실상 기본값 |

    - 비결정적(stochastic): 실행할 때마다 결과가 다름. 시드를 고정해야 재현됨
    - 다양성: 같은 프롬프트로 여러 후보를 만들 수 있음
    - 반복 탈출: 확률 궤도에 갇히지 않아 자연스러운 생성이 나옴
    - 대신 가끔 품질 낮은 토큰을 뽑아 사실 오류나 문맥 이탈이 생길 수 있음 (top-p·top-k로 억제)

6. Greedy decoding (탐욕적 디코딩) vs Sampling (샘플링) 비교

| 축 | Greedy | Sampling |
|---|---|---|
| 선택 규칙 | argmax (1위 고정) | 확률에 비례한 무작위 추출 |
| 결정성·재현성 | 결정적, 항상 재현 | 확률적, 시드 고정해야 재현 |
| 다양성 | 없음 (매번 동일) | 있음 (T·k·p로 조절) |
| 반복·degeneration | 잘 빠짐 | 잘 안 빠짐 |
| 사실 정확도 | 안정적이나 밋밋 | 파라미터 잘못 잡으면 흔들림 |
| 주요 파라미터 | 없음 | temperature, top_k, top_p |
| 관계 | temperature = 0 인 sampling의 극한 | greedy를 일반화한 것 |

    - Greedy (또는 temperature=0): 분류·추출·라벨링, 정답이 하나인 짧은 QA, 형식이 고정된 출력(JSON), 평가·벤치마크처럼 재현성이 중요한 경우, 코드·수식처럼 정확도가 우선인 경우
    - Sampling (top-p 0.9~0.95, temperature 0.7~1.0): 대화, 글쓰기, 브레인스토밍, 요약의 표현 다양화, 한 프롬프트로 여러 후보를 뽑아 고르는 경우

7. Decoding strategy (디코딩 전략)
자기회귀(autoregressive) 생성에서 모델은 매 스텝 다음 토큰의 확률분포만 내놓는다. 이 분포에서 실제로 어떤 토큰을 고를지 정하는 규칙이 디코딩 전략이다. 고른 토큰을 뒤에 붙이고 다시 넣는 과정을 종료 조건까지 반복한다. 모델 가중치는 전혀 건드리지 않는 추론 시점의 설정이지만, 같은 모델도 전략에 따라 완전히 다르게 행동한다. 크게 두 갈래다.

| 갈래 | 방식 | 대표 | 성격 |
|---|---|---|---|
| 탐색(search) | 확률이 높은 시퀀스를 찾음 | greedy, beam search | 결정적, "가장 그럴듯한 하나" |
| 샘플링(sampling) | 분포에서 확률적으로 뽑음 | temperature, top-k, top-p | 비결정적, 다양성·자연스러움 |

8. Beam search (빔서치)
매 스텝 1위 하나만 고르는 대신, 누적 확률이 높은 후보 시퀀스 k개(beam width, num_beams)를 동시에 들고 가며 확장하는 방식이다.
    - 스텝마다 각 빔에서 가능한 다음 토큰으로 뻗어 k × 어휘크기 만큼의 후보를 만들고, 누적 로그확률 상위 k개만 남긴다
    - EOS가 나온 빔은 완성 후보로 따로 보관한다
    - 끝에서 점수가 가장 높은 시퀀스를 최종 출력으로 낸다
    - num_beams=1이면 greedy와 같다

ㄱ) 사용하는 이유
greedy의 근시안(myopic) 문제를 완화한다. 지금은 2위지만 뒤가 더 좋아지는 경로를 놓치지 않는다. 번역·요약처럼 "정확한 하나의 좋은 답"이 목표이고 BLEU·ROUGE로 평가하는 과제에서 greedy보다 낫다.

ㄴ) 한계
    - 연산·메모리가 k배로 느림
    - 열린 생성(대화·창작)에서는 밋밋하고 반복적임. 짧고 안전한 문장이 누적확률에서 유리한 likelihood trap 때문이고, length penalty(길이 정규화)로 일부 보정한다
    - 여전히 결정적이라 다양성이 없음
    - 그래서 현대 LLM 챗/생성에서는 거의 안 쓰고, seq2seq 번역·요약 모델에서 주로 씀
    - 변형: diverse beam search, constrained beam search(특정 단어 강제 포함)

9. Deterministic output (결정적 출력)
같은 입력(프롬프트 + 설정)에 대해 항상 같은 출력이 나오는 성질이다.

| 전략 | 결정적인가 |
|---|---|
| greedy | 예 |
| beam search | 예 |
| sampling (temperature>0) | 아니오 — 매번 다름. 단 시드 고정 + 설정 동일하면 재현 가능 |
| temperature = 0 | 사실상 greedy → 예 (아래 단서) |

ㄱ) 중요한 이유
    - 평가·벤치마크: 점수를 비교하려면 출력이 고정돼야 교란 변수가 안 생김
    - 디버깅·회귀 테스트: 버그를 재현하고, 배포 전후 같은 입력의 출력을 짝지어 비교
    - 캐싱: 같은 입력에 같은 답이면 결과를 재사용할 수 있음

ㄴ) temperature=0
완전한 결정성은 보장되지 않는다.
    - 부동소수점 비결정성: GPU 커널의 합산 순서나 배치 크기에 따라 logits가 미세하게 달라져, 1·2위가 근소하면 tie-break가 갈릴 수 있음
    - MoE 모델: 배치 구성에 따라 expert 라우팅·토큰 드롭이 바뀜
    - 하드웨어·라이브러리·모델 버전 차이

ㄷ) 재현성 확보
temperature=0(또는 낮게) + seed 고정 + 모델 revision 고정 + 라이브러리 버전 기록.

10. Temperature (온도)
softmax를 취하기 전에 logits를 T로 나눠 분포의 뾰족한 정도를 조절한다. 예: logits [3.0, 2.0, 1.0]

| T | 분포(대략) | 성격 |
|---|---|---|
| 1.0 | [0.67, 0.25, 0.09] | 원본 그대로 |
| 0.5 | [0.87, 0.12, 0.02] | 뾰족 — 1위에 쏠림, 보수적·일관적 |
| 2.0 | [0.51, 0.31, 0.19] | 평평 — 하위 토큰 기회↑, 과감·창의적·위험 |
| → 0 | [1, 0, 0] | argmax = greedy |
| → ∞ | 균등분포 | 완전 무작위 |

temperature만 올리고 top-k/top-p로 안 자르면 꼬리의 쓰레기 토큰이 튄다. 보통 함께 쓴다. 적용 순서는 대개 temperature로 분포를 데운 뒤 top-k/top-p로 자른다(구현마다 조금 다름).

11. Top-k sampling
확률 상위 k개 토큰만 후보로 남기고 나머지는 잘라낸 뒤(확률 0), 남은 것을 재정규화해 샘플링한다.
    - k=1이면 greedy, 흔한 기본값은 k=40~50
    - 장점: 꼬리의 이상한 토큰을 원천 차단
    - 단점: k가 고정이라 분포 모양에 안 맞음
        - 분포가 뾰족할 때(정답이 거의 확실): 후보 50개 중 48개가 쓸데없이 포함 → 가끔 엉뚱한 선택
        - 분포가 평평할 때(여러 개가 그럴듯): 50개로도 좋은 후보를 잘라버릴 수 있음

12. Top-p sampling (nucleus sampling)
확률을 큰 순으로 누적해서 합이 p를 처음 넘는 지점까지의 토큰만 후보로 삼는다(이 후보 집합을 nucleus라 부른다).
    - 후보 개수가 분포 모양에 따라 자동 조절됨
        - 뾰족한 분포: 토큰 1~2개로 0.9 도달 → 후보 적음 (greedy에 가까워짐)
        - 평평한 분포: 20~30개 필요 → 후보 많음 (다양성 확보)
    - top-k의 "고정 k" 약점을 보완. 현재 열린 생성의 사실상 기본값 (보통 p=0.9~0.95)
    - top-k와 함께 쓰기도 함 (top-k로 상한을 두고 top-p로 실제 컷)
    - 변형(optional): min-p(1위 확률 대비 비율로 컷), typical-p

13. Generation config (생성 설정)
생성 파라미터를 한데 묶은 설정이다. Hugging Face는 generation_config.json 파일, API는 요청 파라미터로 넘긴다.
    - 모델 제작자가 권장하는 기본값이라 체크포인트·모델 카드에 함께 배포됨
    - 재현성: 이 설정 + seed + 모델 revision을 고정해야 결과가 재현됨
    - 같은 모델도 config에 따라 체감 성능이 완전히 달라져서, 평가할 때 반드시 명시해야 함
    - 흔한 함정: base 모델은 generation_config가 부실해 직접 지정해야 하고, chat/instruct 모델은 대체로 잘 세팅돼 있음

14. Diversity vs coherence (다양성 vs 일관성)
디코딩 파라미터를 정하는 일은 결국 이 둘의 상충 관계(trade-off)를 조율하는 것이다.

| 방향 | 설정 | 결과 | 실패 모드 |
|---|---|---|---|
| 일관성 쪽 | 낮은 T, 작은 k/p | 안전·정확·일관 | 밋밋함, 반복 루프(degeneration) |
| 다양성 쪽 | 높은 T, 큰 k/p | 창의적·다채로움 | 문맥 이탈, 사실 오류(hallucination), 횡설수설 |

    - 사실 QA·코드·추출: 일관성 우선 → T 0~0.3, greedy 근처
    - 일반 대화: 균형 → T 0.7, top-p 0.9
    - 창작·브레인스토밍: 다양성 우선 → T 0.9~1.2, top-p 0.95

ㄱ) 측정 지표(optional)
다양성은 distinct-n(고유 n-gram 비율)·self-BLEU, 일관성은 perplexity·사람 평가·LLM-as-judge.

ㄴ) 핵심 흐름
top-p·min-p 같은 truncation 기법이 이 상충 관계를 완화한다. 꼬리의 쓰레기 토큰은 자르되, 몸통에 있는 그럴듯한 후보들의 다양성은 살리는 방향이다. 더 나아가 품질 자체를 높이려는 contrastive decoding, DoLa 같은 방법도 있다(advanced).

15. Chat message (채팅 메시지)
요즘 instruct/chat 모델은 평문 프롬프트가 아니라 역할(role)이 붙은 메시지들의 리스트로 입력을 받는다.

    [
      {"role": "system",    "content": "너는 리뷰 감성 분류기다. positive/negative/neutral 중 하나로만 답하라."},
      {"role": "user",      "content": "이 영화 정말 최고였다"},
      {"role": "assistant", "content": "positive"},
      {"role": "user",      "content": "돈이 아까웠다"}
    ]

    - 기본 역할은 system, user, assistant 세 가지 (최근엔 도구 호출용 tool 역할도)
    - 대화는 보통 system 하나로 시작해 user ↔ assistant가 번갈아 이어짐
    - 왜 이 형식인가: 모델이 명령어 튜닝(instruction tuning)·선호 최적화 때 이 구조로 학습됐기 때문. 누가 말했는지 구분돼야 지시-응답 관계, 안전 정렬, 멀티턴 문맥이 동작함
    - 이 리스트는 채팅 템플릿(chat template)을 거쳐 하나의 토큰 시퀀스가 됨. 특수 토큰으로 역할 경계를 표시하며 모델마다 형식이 다름. API는 서버가 자동 적용, 로컬은 tokenizer.apply_chat_template()

16. System message (시스템 메시지)
대화 맨 앞에 한 번 놓여 모델의 정체성·역할·규칙·톤·제약·출력 형식을 지정한다.
    - 내용 예: "너는 ~다"(페르소나), 안전 가이드, "JSON으로만 답하라", 현재 날짜·맥락 주입
    - 보통 사용자에게 안 보이고 앱 개발자가 설정함
    - 대화 내내 유지되는 안정적 뼈대
    - instruction 계층에서 우선순위가 높음 (모델이 system > user로 학습됨). 다만 완벽하지 않아 프롬프트 인젝션에 뚫릴 수 있음
    - 일부 모델은 system을 별도로 안 받고 첫 user에 합침
    - 평가·기준선에서는 모든 예시에 동일하게 고정

17. User message (유저 메시지)
사람(또는 앱)이 모델에게 보내는 입력·질문·지시다.
    - 매 턴 바뀌는 부분이고, 마지막 user 메시지가 지금 답해야 할 것
    - 실제 과제 입력(분류할 문장, 요약할 글 등)이 여기 들어감. 구분자(XML 태그 등)로 지시와 입력의 경계를 나누면 인젝션 방어에 좋음
    - few-shot으로 예시를 넣을 때 데모의 "질문" 쪽도 user 역할로 들어감

18. Assistant message (어시스턴트 메시지)
모델이 생성하는 응답이다(role: "assistant").
    - 멀티턴에서 이전 assistant 메시지들은 대화 히스토리로 다시 입력에 포함됨. 모델은 상태가 없어서(stateless) 매 호출마다 전체 대화를 통째로 받음
    - few-shot 데모에서 "정답" 쪽이 assistant 역할
    - SFT 학습 때 손실은 assistant 토큰에만 계산하고 system·user 부분은 마스킹함 (NOTE/4-LLM.md 손실 마스킹)
    - optional: assistant 메시지를 미리 일부 채워 넣어 모델이 그 뒤를 이어 쓰게 하는 prefill 기법이 출력 형식 강제에 쓰이고, 도구 호출(tool call)도 assistant 메시지의 특수 형태

19. message 정리

| 구분 | 만드는 주체 | 언제 | 사용자에게 | 학습 시 손실 | 프롬프트 템플릿 매핑 |
|---|---|---|---|---|---|
| system | 앱 개발자 | 맨 앞 1회 | 안 보임 | 마스킹 | Role + Task + Output Schema |
| user | 사람/앱 | 매 턴 | 보임 | 마스킹 | few-shot 질문 · 마지막 Input |
| assistant | 모델 | 응답마다 | 보임 | 계산 대상 | few-shot 정답 |
